[project]
name = "some-database"
version = "0.1.0"
description = "Learning how to write a database"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.11.4",
    "rich>=14.0.0",
    "sqlparse>=0.5.3",
    "streamlit>=1.49.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.8",
]
